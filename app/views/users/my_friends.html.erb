<%= render 'friends/lookup' %>
<% if @friendships.size > 0 %>
  <%= render 'friends/list' %>
<% else %>
  <div class="row-col-lg-12">
    <p class="lead">You don't have any friends yet. Pleas add some!</p>
  </div>
<% end %>